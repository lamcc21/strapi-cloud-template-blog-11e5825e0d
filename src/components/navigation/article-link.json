{
  "collectionName": "components_navigation_article_links",
  "info": {
    "displayName": "Article Link",
    "description": "A navigation link that references an article with optional custom path"
  },
  "options": {},
  "attributes": {
    "article": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::article.article",
      "required": true,
      "description": "Select a standalone article to include in the dropdown"
    },
    "custom_path": {
      "type": "string",
      "description": "Optional custom URL path (e.g., 'insights/ai-accelerator'). If empty, uses '/article-slug'"
    },
    "order": {
      "type": "integer",
      "default": 0,
      "description": "Display order in dropdown. Lower numbers appear first."
    }
  }
}